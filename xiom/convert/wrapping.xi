// XIOM - Conversion: Wrapping
// Copyright (c) 2026 Eleftherios Notas
// Licensed under the MIT or Apache-2.0 license, at your option.

module xiom.convert.wrapping

// Depends on: xiom.num

// ============================================================================
// Wrapping integer arithmetic that truncates on overflow. NOTE: current
// implementation lives in num - move the functions here during the
// implementation phase. TODO(compiler): implement.
// ============================================================================

// fn wrapping_add(a: Int, b: Int) -> Int - add, wrapping on overflow. TODO(compiler): implement.
// fn wrapping_sub(a, b) -> Int - subtract, wrapping on overflow. TODO(compiler): implement.
// fn wrapping_mul(a, b) -> Int - multiply, wrapping on overflow. TODO(compiler): implement.
// fn wrapping_neg(a) -> Int - negate, wrapping on overflow. TODO(compiler): implement.
// fn wrapping_abs(a) -> Int - absolute value, wrapping on overflow. TODO(compiler): implement.
// fn wrapping_shl(a, n) -> Int - shift left, discarding shifted-out bits. TODO(compiler): implement.
// fn wrapping_shr(a, n) -> Int - shift right, discarding shifted-out bits. TODO(compiler): implement.
