// XIOM - Sorting: Quick Sort
// Copyright (c) 2026 Eleftherios Notas
// Licensed under the MIT or Apache-2.0 license, at your option.

module xiom.sort.quick

// Depends on: none

// ============================================================================
// Quick sort family: divide-and-conquer with pivot partitioning.
// NOTE: current implementation lives in sort.xi - move the functions here
// during the implementation phase. TODO(compiler): implement.
// ============================================================================

// fn quick_sort(v: &mut Vec[Int]) - in-place quicksort of an Int vector. TODO(compiler): implement.
// fn quick_sort_range(v, lo, hi) - quicksort the inclusive range v[lo..=hi]. TODO(compiler): implement.
// fn quick_sort_by[T](v: &mut Vec[T], compare: fn(&T, &T) -> Int) - quicksort with a custom comparator. TODO(compiler): implement.
// fn partition(v, lo, hi) -> Int - Lomuto/Hoare partition; returns the pivot index. TODO(compiler): implement.
// fn quick_select(v: &mut Vec[Int], k) -> Int - returns the k-th smallest element, reordering v. TODO(compiler): implement.
// fn quick_sort_3way(v: &mut Vec[Int]) - quicksort with 3-way partitioning for duplicates. TODO(compiler): implement.
