// XIOM - Collections: Ring Buffer
// Copyright (c) 2026 Eleftherios Notas
// Licensed under the MIT or Apache-2.0 license, at your option.

module xiom.collect.ring

// Depends on: none

// ============================================================================
// Bounded single-producer single-consumer lock-free ring buffer of Int
// elements.
// NOTE: current implementation lives in collect.queue SpscRing - move the
// functions here during the implementation phase. TODO(compiler): implement.
// ============================================================================

// fn ring_new(cap: Int) - create a bounded ring with the given capacity. TODO(compiler): implement.
// fn ring_push(r: &mut SpscRing, value: Int) -> Bool - try to enqueue, failing when full. TODO(compiler): implement.
// fn ring_pop(r: &mut SpscRing) -> Option[Int] - dequeue a value, None when empty. TODO(compiler): implement.
// fn ring_len(r: &SpscRing) -> Int - number of buffered elements. TODO(compiler): implement.
// fn ring_is_empty(r: &SpscRing) -> Bool - check whether the ring has no elements. TODO(compiler): implement.
// fn ring_capacity(r: &SpscRing) -> Int - maximum number of buffered elements. TODO(compiler): implement.
