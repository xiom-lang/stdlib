// XIOM - Array: Dynamic
// Copyright (c) 2026 Eleftherios Notas
// Licensed under the MIT or Apache-2.0 license, at your option.

module xiom.array.dynamic

// Depends on: none

// ============================================================================
// Dynamic vector (Vec[T]) helpers: mutation, concatenation, search.
// NOTE: current implementation lives in array.xi - move the functions here
// during the implementation phase. TODO(compiler): implement.
// ============================================================================

// fn array_push[T](a: &mut Vec[T], value) - append value to the end. TODO(compiler): implement.
// fn array_pop[T](a) -> Option[T] - remove and return the last element, or None if empty. TODO(compiler): implement.
// fn array_insert[T](a: &mut Vec[T], idx, value) - insert value at idx, shifting later elements. TODO(compiler): implement.
// fn array_remove[T](a: &mut Vec[T], idx) -> Option[T] - remove and return the element at idx, or None if out of bounds. TODO(compiler): implement.
// fn array_resize[T](a: &mut Vec[T], n, fill: T) - resize to n, padding new slots with fill. TODO(compiler): implement.
// fn array_concat[T](a, b) -> Vec[T] - new vector with a followed by b. TODO(compiler): implement.
// fn array_extend[T](a: &mut Vec[T], b: &Vec[T]) - append all elements of b to a. TODO(compiler): implement.
// fn array_sort[T](a: &mut Vec[T]) - sort a in place. TODO(compiler): implement.
// fn array_search[T](a, item) -> Option[Int] - index of the first occurrence of item, or None. TODO(compiler): implement.
// fn array_is_empty[T](a) -> Bool - true if the vector has no elements. TODO(compiler): implement.
