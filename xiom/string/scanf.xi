// XIOM - String: Scanf
// Copyright (c) 2026 Eleftherios Notas
// Licensed under the MIT or Apache-2.0 license, at your option.

module xiom.string.scanf

// Depends on: xiom.fmt

// ============================================================================
// Scan a string against a format specifier to extract typed values. NOTE:
// current implementation lives in fmt.sscanf - move the functions here during
// the implementation phase. TODO(compiler): implement.
// ============================================================================

// fn str_scanf(s: Str, spec: Str) -> Result[Vec[Str], Str] - parse s per spec, extracting the raw matched fields. TODO(compiler): implement.
// fn str_scanf_ints(s: Str, spec: Str) -> Result[Vec[Int], Str] - parse s per spec, extracting Int values. TODO(compiler): implement.
// fn str_scanf_floats(s: Str, spec: Str) -> FloatScan - parse s per spec, extracting Float64 values. FloatScan: a struct holding the parsed Float64 values and any unconsumed remainder of s. TODO(compiler): implement.
